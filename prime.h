// prime.h

// returns true if n is a prime number, and false otherwise
bool is_prime(int n);

// returns if n is a composite number, and false otherwise; an integer is
// composite if it's greater than 1 and not prime
bool is_composite(int n);

// returns the number of primes less than n
int num_primes_less_than(int n);
